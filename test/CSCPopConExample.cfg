process ProcessOne = {
 service = MessageLogger {
    untracked vstring destinations = {"cout"}
    untracked PSet cout = {
      untracked  PSet default = { untracked int32 limit = 0 }
    }
 }

//Subproject specific modules
     source = EmptyIOVSource {
         string timetype = "runnumber"
         untracked uint32 firstRun = 1
         untracked uint32 lastRun = 1
         uint32 interval = 1
       }

es_source = CSCFakePedestalsConditions{}
es_prefer = CSCFakePedestalsConditions{}


//PopCon config
include "CondCore/PopCon/data/PopConDB.cfi"
// to write in sqlite
# replace PopConDB.connect = "sqlite_file:DB_test.db"
// to write into db
  replace PopConDB.connect = "oracle://rls1/CSC_COND" 

// Database output service
   service = PopConDBOutputService {
       using PopConDB
       VPSet toPut = { { string record = "CSCPedestalsRcd" string tag = "CSCPedestals_test20" } }
   }


//Common parameters to all subprojects
module Test1 = CSCTestAnalyzer{
   using PopConDB
   bool SinceAppendMode = true
   bool debug = true
}
 path p = {Test1}
}

